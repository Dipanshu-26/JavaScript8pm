export default class mathUtility{
    static baseurl = "https://www.amazon.in/"
    static pi = 3.14
    static username = "dipanshu"
    static password = "dip@123"
    static marks = [56,89,99,100,67]

    //add
    static add(x, y) {
        console.log(x + y)
    }

    static sub(x, y) {
        console.log(x - y)
    }
    static mul(x, y) {
        console.log(x * y)
    }
    static div(x, y) {
        console.log(x / y)
    }
    static mod(x, y) {
        console.log(x % y)
    } 
}