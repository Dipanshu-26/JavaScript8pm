export default class loginUtils {
    baseurl = "https://www.amazon.in/"
    pi = 3.14
    username = "dipanshu"
    password = "dip@123"

    // constructor(uid,pw){
    //     this.uid = uid
    //     this.pw = pw
    //     console.log(`uid= ${this.uid} pw = ${this.pw}`)
    // }
    //add
    add(x, y) {
        console.log(x + y)
    }

    sub(x, y) {
        console.log(x - y)
    }
    mul(x, y) {
        console.log(x * y)
    }
    div(x, y) {
        console.log(x / y)
    }
    mod(x, y) {
        console.log(x % y)
    }
}

//export default loginUtils